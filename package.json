{
  "scripts": {
    "dev:ff": "npm run ff",
    "dev:edge": "npm run edge",
    "dev:chrome": "npm run chrome",
    "ff": "web-ext run --target firefox-desktop",
    "edge": "web-ext run",
    "chrome": "web-ext run --target chromium ",
    "pack:ff": "web-ext sign --api-key=$API_KEY --api-secret=$API_SECRET --channel unlisted",
    "lint": "",
    "lint:fix": ""
  },
  "dependencies": {
    "web-ext": "^8.3.0"
  }
}
